# DECISION-FRAMEWORK — ゆうの判断モデル（事実ベース）

> **これは何か**: あの日 (CHANGELOG v1〜v233・75日) と 🧾レシート (r1〜r91・11日)、ライフログ (owntracks-supabase-notion)、動画版構想 (Notion) — 複数プロダクトの開発記録から抽出した「**どういう文脈の時に、どう判断してきたか**」のモデル。全主張に出典 (vN / rN / 日付) を付ける。出典のない一般論は書かない。
> **いつ使うか**: 新機能・新プロダクト・技術選定・UX・リリースで迷った時に、§2 の該当する文脈を開いて照らす。**Claude はスコープや設計の相談・提案の前にここを参照する**。
> **これは何でないか**: ルールブックではない。過去の判断の蒸留であって、未来を縛る契約ではない。本人は証拠が来れば自分の決定を覆す (§5)。このモデル自体も同じ扱いでよい。

---

## 1. 判断の憲法（最上位原理・7ヶ条）

すべての文脈判断 (§2) の上位にあり、衝突したらこちらが勝つ。

### ① コア一行がリトマス試験紙
プロダクトごとに「コア」を一行で定義し、機能・広告・AI・名前・コピーの全判断を「**コアを強めるか / 壊すか**」で下す。
- あの日 = 「偶然よみがえった久しぶりの記憶」(3要素: 偶然/久しぶり/よみがえる、2026-05-29 確定)。レシート機能は3要素とも逆向き → 同居せず別アプリ (v233)。店頭コピーも操作でなく payoff「久しぶりがよみがえる」を主役に (v152)。IG 取り込みの日時に「ダウンロード日」を使う案は「全部『今』に寄り久しぶりが壊れる」で却下 (v43)。
- コアに入れる範囲も規律的: 「何かが始まる (次の旅)」は派生効果でありコアに入れると過剰約束 → 軸から外す (Notion WHY §0)。

### ② 実機の手触りが最終判定器
設計案は紙の上では決まらない。触って初めて決まる (v5)。preview/E2E green は必要条件であって十分条件ではない — ほぼ全版に「実機の手触りは残」と書き分けてきた。閾値・パラメータは定数に出して「実機で調整する前提」で出荷する (v180-182, r48)。実運用でしか出ないフィードバックがある (r44「52/52 green のまま素通り」)。

### ③ park / un-park の二重ゲート
迷ったら「これは今すぐ (A) か?」→ A 以外は「今は作らない」リストへ、**but 書き（回避策と再開条件）付きで** park (v1 以来)。un-park は「**検証 YES ＋ 本人の明示要求**」が両方揃った時だけ: 除外✕ (v13)、足跡 (v39)、地図ビュー (v49)、シェア (v194)。un-park しても方式はコア基準で選び直す (v194: シェア解禁でもサーバーは建てず JSON 手渡し)。

### ④ 出荷が常に最上位・審査リスクは構造で消す
push = スマホで触れる、がワークフローの核 (v3 で public 化した理由)。「▶ 次の一手」はほぼ常に出荷系 (実機確認 or 審査提出) が①。承認済みでも第一印象が悪ければリリースしない (build 16 放棄・v128 前提)。審査ゲートは対処でなく構造で回避する: 初回は位置を依存ごとビルドから除外 (v104→v106)、Android は ACCESS_BACKGROUND_LOCATION を宣言しない FGS 方式 (v150/v168)、EU27 除外 (2026-07-03)、BYOK 審査には専用キーの台本 (r72)。

### ⑤ 可逆・非破壊・最小差分
削除でなくフラグ (excluded v13)、推定は derived で実データに混ぜない (v74)、改名は表示だけで内部識別子とDBは不変 (v76, v145)、スキーマは追加のみ・「同居＋タグ＋述語」がこのコードベースの勝ちパターン (v194)。出荷済み・審査中のコードには触らない (v216: iOS 審査中は Android 限定ゲートで対処)。撤回する時も全部は巻き戻さず、要素ごとに残す/戻すを決める (r48, r89)。

### ⑥ 嘘をつかない・沈黙しない・捏造しない
ボタンの名前は仕様書 (r13)。0件・0年分を正直に出す (v87, v125)。取れない数字は捏造せずトークン数だけ出す (r59)。二重課金は二重課金と言う (r70)。「もう直らない」とUIに書くのは削除を勧めることになる → 断定しない (r76→r77)。機能を足すと既存の文言が静かに嘘になるので、文言を全部たどり直す (r80)。プライバシーは「出ない方向に壊れる」構造で守る (v194, v198)。

### ⑦ 文書が memory・誤りも日付つきで残す
口頭の文脈は失われる。TODO の現在地と CHANGELOG (背景/設計判断/教訓) が唯一の引き継ぎ。方針転換は「✅ 決定 (日付) ＝旧方針を上書き」、自分の誤りは「⚠️訂正」として残す (v106, v150, r58, r77)。この文化があるから、判断は3リポ (あの日/レシート/ライフログ) を跨いで移植できる (§2.9)。

---

## 2. 文脈別プレイブック — 「この文脈ではこう判断してきた」

### 2.1 新しいプロダクトのアイデアが出た
1. **既存アプリのコア一行と照合**する。3要素が逆向きなら同居させず別アプリ (v233 レシート)。実装・運用・リスクの性質が非対称でも別プロダクト (Notion 動画版: リモート依存/外部再生/規約支配の3点で分離)。
2. ただし「**リポの同居とアプリの同居は別の話**」(v233) — 検証ループ (push→iPhone Safari) が初日から使えるなら同リポのサブフォルダでよい。運用は完全独立 (別 BUILD 連番・別 TODO/CHANGELOG・DB名/接頭辞分離)。
3. 構想段階では Notion に「構想止め」ページを作り、事実に confidence (high/medium/low) を付ける。最優先で潰すべき実測点と最小 PoC を一枚に書いて寝かせる (動画版 2026-06-07)。
4. 着手したら**初日にフルループを一周**させる (r1: 撮る→AI→一覧→書き出しを1日で)。1つ目 (あの日) は体験の手触り検証が先だったが、2つ目以降はデータが一周する配管が先。
5. **データがゼロのうちに、過去の自分の実装から実測教訓を移植**してスキーマを直す (r2: 「データが1件もない今がスキーマ修正の最安タイミング」「同じ問題を先に解いた自分の実装が最強の参考資料」)。

### 2.2 新機能の要望・アイデアが出た
1. まず「今すぐ (A) か?」→ A 以外は「今は作らない」へ (§1③)。**要望が出る前の先回り実装はしない** (v231「要望が出たら検討」が口癖レベル)。
2. **要望を機能追加に直訳しない**。既にあるか・可視化で足りるかを先に疑う (v22 差分ボタン→既存 dedup の可視化 / v48 削除復活→既存フラグに一覧UIを被せるだけ)。
3. 本人が列挙した選択肢が4つでも、**成立する挙動は2つ**だったりする — broken な組み合わせを作らない形に整理してから実装 (v53)。複数の要望が**1つの概念に集約**できないかを見る (v207: 3要望→時刻アンカー1概念 / r73: 繰り返し登録とコピペ→「もう一度作る」1動詞)。
4. コアを**強める方向**の要望は即拡張してよい (v195-197 贈り物ビルダー: 「選ぶ・一言添える行為自体が想起」)。拡張中も細かい見送りは積む (v195 常設キャプション見送り)。
5. 提案の**字面でなく狙い**で作る (r43: 「トーストで設定を開く」→押した人だけ開く形に変えて通った)。ユーザー提案でも文脈が合わなければ理由を付けて修正して採る (v200 「共有」→「表示/非表示」、v204 配置)。
6. 精度・品質が実用に達しないものは**看板から下ろす**（機構は再挑戦用に残置してよい）: 意味レンズは「食べ物だけ」に凍結、再挑戦はより強いモデルとセットでのみ (v160/v163)。

### 2.3 UI に情報・操作を足したい
1. 毎回「**これは思い出しの意識を写真から逸らさないか**」(memory/ui-minimalism-works)。既定は最小、**使う時だけ広がる**: 長押し (v6)、開閉式 (v40)、バッジは0で隠す (v48)、空状態にだけ出るヒント (v185)。
2. **選択肢UI・設定の追加は最終手段**。「複雑になるだけ」(v58) — 良い既定＋視覚エンコード (色=日) や「持ち方で自動」(v173) に倒す。
3. **clever より予測可能**: 賢い非表示ゲートはユーザーには「壊れた」と映る (v79→v80)。空ケースは飛び先で穏当に degrade。
4. **一等地は使用頻度で決める** (v99)。実機FBは機能追加より「動線の深さ」への要求が多い — コア操作は階層を浅く (v153/v155: 5〜6手順→1タップ)。
5. **自動と手動は両取り**: 初期値は自動で即結果、微調整は手動でライブ数値を見せる (v154)。自動判定に迷いがあるなら「重なったら必ず聞く」(v143)、AIに任せるなら判定理由を必ず見せる (r68)。**候補を出すことと勝手にやることは別** — 既定値の置き方で解く (r81)。
6. 危険な操作は Undo でなく「押す前に見せる」に投資し、そもそも**作れなくする** (r71 件数表示、r66 重なる時間帯は選択肢に出さない)。ハード削除はしない (v13, v231)。

### 2.4 技術選定
1. **最少手数で最大の未知を先に潰す**: A案 (コミュニティ製・1ビルドで可否判定) → capability YES が出てから B案 (自前・本命) (v92→v93)。「真の検証は最初の1ビルドでしか分からない＝だから早く回す」(v89)。
2. **既存資産の流用・downstream 無改修**が第一候補 (v94: web と同一形の record / v142: 契約を iOS と1文字も変えず Android 実装 / v219: 出荷済み UI 部品の流用)。新しいUIやストアを発明しない。
3. 外部サービス・API は**事前対応表を持たず「頼んでみて断られたら言い直す」**3段構え (r9-r10)。相手が受け取れるかは推測せず端末・原典に聞く (r8 adb dumpsys, v216 SystemBars.java 精読)。
4. 制約は器を変えて解く: ITP でデータが消える → サーバー保存ではなく native 化 (r4「ログインだとデータがサーバーに置かれる、それはまずい」)。web で不可能なことは早めに正直に伝えて native の宿題に切る (v22, v24, v73)。
5. 道具の役割は現実に合わせて再定義してよい (v208: Codemagic は iOS 専用・Android は手元ビルド)。**最終形が確定している暫定には投資しない** (TODO 🔑: localStorage→Keychain)。

### 2.5 データ・プライバシー
1. 原則の適用は**データの機微度と「漏洩時の被害の質」(金銭か・プライバシーか) で決める** (TODO 🔑 2026-08-10)。あの日=写真は端末から出ない (絶対)。レシート=BYOK で外に出るが**サーバーレス＝運営者は触れない**形で精神を継承 (r1)。最機微 (体の写真) は「どこにも出ない」こと自体が価値 (r85)。
2. **約束と同居できない機能は別アプリ** (v233)。プライバシーポリシーは「コピーして名前を変える」ができない — 性質が逆なら骨から書き直す (r72)。
3. 守り方は注意書きでなく構造: 既定の述語を厳しくし例外側を明示 opt-in (v194 visible/mapVisible)、渡す単位の意味に既定を合わせる (v198 場所パックは円内クリップ)、書き出しに位置を焼き込まない (v165)。
4. 二重管理は必ずズレる — DB直結・コードコピーはしない。共有は「OS写真ライブラリ＋ファイル契約」のような狭い契約で (v233、madeleine/spike 統合 2026-06-17 の教訓)。

### 2.6 AI の使い方
1. AI を使うかはコアで決める: あの日は on-device のみ (opt-in・端末内)。**「AI を使うアプリでも、使わない判断がいちばん効く場所がある**」— 既に答えがあるものに二度払わない (r80: 料金・待ち時間・精度の3つとも良くなった)。
2. **材料は AI、判定はアプリ、数字は作らせない**: 税込/税抜は数字から決める (r24)、迷ったら null と言わせる (r24/r68)、カロリーは出させない (r35)、原本の数字は勝手に直さない (r22)。
3. AI の不完全さは味方にもなる: 完璧な整列より**ノイズ混じりの mix が報酬** (v19「混ざったほうがおもしろい、単体だと退屈」)。逆に精度不足は看板から下ろす (v163)。
4. BYOK では**課金が第一級の設計変数**: リトライ可否は「お金が動いたか」で決める (r51/r77/r78)、押す前に値段を見せる (r59)、エラー文言の正確さがそのまま金額になる (r17)。
5. プロンプトは使い道から逆算して絞る。出力がおかしい時はモデルでなくプロンプトを「出力の目線」で読み直す (r44)。

### 2.7 バグ・不可解な挙動
1. **測ってから直す・推測の連鎖を切る**: 診断カウンタ・区間計測で犯人を確定 (v37, v226, v8-9)。原因が端末で割れるならフレームワークのソースやバイナリ現物まで潜る (v216, v215)。
2. **診断道具を先に作る** — 失敗の控えは人の宿題でなくアプリの仕事 (r30)。「次の失敗が一撃で情報になる」(r55)。自作の点検が自分の直しを否定することもある — それが点検の効果 (r76→r77)。
3. **1回の修正で直すのは1つ** (r47→r48: 2つ直すとどちらが効いたか分からない)。数字が指した1つだけを直す。
4. **「確定」と書く前に観測した窓の広さを言う** (r58)。一時的な不調と恒久的な欠陥は次の手が同じでも書き方を変える。本人しか持っていない材料がある (r58「前日は通っていた」)。
5. 片側 (iOS/Android, web/native) だけ直した過去の対処は、**もう片側にも同症状が潜む** — FB が来たら対称性を疑う (v220→v230)。
6. 同じ場所に2回混乱FBが落ちたら、機能でなく説明不足のサイン (v185)。同じ失敗を複数回踏んだら注意書きでなく構造を変える (r49/r50)。

### 2.8 リリース・審査・ストア
1. **リスク面を初回に入れない**: 位置オフ v1 (v104-106)、iPhone 専用 (v109)、クリーン版スパイク (v141)。落とすのは削除でなくフラグ＋依存除外の二層 (v106 の学習: JS で呼ばないだけでは静的スキャンは欺けない)。
2. **train の定石**: 公開済み版に新ビルドは足せない→新 train に分離 (1.5→1.6→1.7)。バージョン変更を入れてから CI を回す (2026-07-17 方針)。審査中のビルドに触れない (v130)。
3. 提出前プリフライト: 新権限ゼロ確認・sync 済み・CDN 残留ゼロ・バンドル内 BUILD 文字列で「web が本当に載った証拠」(vc3/vc4)。審査メモには差分と**非変更** (新権限ゼロ・前回と同じ回答) を明記。
4. 待ちを遊ばせない: 審査待ち中に並行トラック (i18n 先行、Android 着手)。**待つ/待たないは共有リスク面があるかで決める** (2026-07-03)。クリティカルパス (登録承認待ち) は最初に申し込む (Phase 0)。
5. リジェクトは最小対処で最短再提出 (v110: 2点とも即日)。最警戒 (4.2) の答え合わせをする。ストア文言・コピーは**自分で使って刺さった体験**を根拠に (v152)。

### 2.9 検証の設計 (spike の始め方・終わり方)
1. Phase 0 は体験でなく**前提データが読めるか**から (v1-v3: EXIF 日時/GPS/HEIC)。ゴールは機能一覧でなく**検証したい問い**で切る (「気持ちいいか」「やめにくいか」)。
2. 重い投資の前に**撤退可能な pilot** (v17: CLIP は 50枚・opt-in・消せる)。変数は1つにする (軽量モデルでなく標準 CLIP で「モデルが弱かったせい」を排除)。
3. **コードを書く前に手で検証できる体験はゼロ行で検証** (r62: Drive→AI を手で一周→役割反転の確信を得てから UI を寄せた)。
4. 深追いの**打ち切りラインは spike の目的**で引く (v37: iPad OOM は「目的は iPhone で達成済み」で撤退)。「一気に綺麗にしない」— 整理は次の文脈で段階的に (v77-78)。
5. 競合調査は機能を足すためでなく「**自分の嘘**」と「**真似しないもの**」を見つけるため (r38-39, r82: ストリーク・push 通知は真似しない＝逆張りが差別化の本体)。

### 2.10 ドキュメント・引き継ぎ・セッション運用
1. セッション終了時に TODO 現在地と CHANGELOG (背景/設計判断/教訓) を必ず更新。再開ポイントは定型 (①web ②iOS ③Android ④待ち無しタスク) で探索コストゼロに。
2. 大きい実装は前セッションで「一気にやる用のプラン」を用意 (継ぎ目の関数:行番号まで)。1セッション=1テーマ、ただし実機FBループは同日に vN 連打。
3. 画面に出す文字列・追記型の文書は**上限を運用ルールに入れる** (v214: BUILD は最新5版)。
4. 判断の移植は「実装」でなく「**判断の理由**」を写す (r91: ライフログの予算バーは判断ごと移植し、採らない判断を1つ決めるだけで済んだ)。前提の差は1つずつ確かめる (r87: 風景の定点前提は体の写真に合わない)。

---

## 3. 迷った時のチェックリスト（上から順に）

1. **コアを強めるか、壊すか？** — プロダクトのコア一行に照らす。逆向きなら同居させない。
2. **これは今すぐ (A) か？** — A 以外は but 書き付きで「今は作らない」へ。un-park は検証 YES＋明示要求。
3. **既にあるもので満たせないか？** — 既存機構の可視化・流用・過去の自分のリポからの判断移植を先に。
4. **最大の未知はどこか？最少手数で先に潰せるか？** — 撤退可能な最小スパイク→本命の2段。ゼロ行の手検証も選択肢。
5. **可逆か？既定は安全側か？** — フラグ/追加のみ/derived/opt-in。出荷済み・審査中に触っていないか。
6. **これは紙の上で決めることか、実機で決めることか？** — 閾値は定数に出して実機で回す。preview green で完了と言わない。
7. **ユーザーに嘘をつかないか？** — 名前と中身の一致・0件の正直表示・課金/データの行き先を黙らない。
8. **失敗した時に何が起きるか？** — 出る方向に壊れないか (プライバシー)・お金が動くか (リトライ)・回復導線はあるか (権限)。
9. **判断を記録したか？** — 採らなかった案と理由、転換なら「旧方針を上書き」と日付。

---

## 4. アンチパターン集（実際に踏んだもの）

| アンチパターン | 何が起きたか | 出典 |
|---|---|---|
| コードのコピー二重管理 | madeleine v38 / spike v75 でズレて統合を強制された。以後 DB 直結共有の却下理由にもなる恒久参照点 | 2026-06-17, v233 |
| 賢い非表示ゲート | 条件付き表示がユーザーには「消えた・壊れた」と映る | v79→v80 |
| フラグで外したつもり | JS で呼ばないだけでは Apple 静的スキャンは欺けない。依存ごと除外が必要 | v104→v106 |
| 片側だけ対処 | Android だけ直した重なりが iOS でも発症。「無影響」と書いた判断の訂正 | v220→v230 |
| 1文字の汎用名をグローバルに | i18n 関数 `t()` が既存ローカル変数と衝突、preview で検出不能なクラッシュ | v130→v132 |
| 1回の修正で2つ直す | effort と上限を同時に変え、どちらが効いたか分からないまま精度が落ちた | r47→r48 |
| 証拠不足で「確定」と書く | 40分の観測窓で経路の問題と断定→前日の反例で撤回 | r57→r58 |
| 「もう直らない」とUIで断定 | 削除を勧める案内になっていた。点検を先に回すべきだった | r76→r77 |
| 人に宿題を出す残課題 | 「失敗したら文言を控えて」は保存し忘れの言い換え。アプリが記録する仕事 | r29→r30 |
| 大ファイルへの貪欲正規表現 | BUILD 更新で 752KB→190KB にファイル破壊。HEAD の写しを先に置く | r90 |
| 選択肢セレクタの安易な追加 | 線モードセレクタを1日で作って翌日全廃。「複雑になるだけ」 | v57→v58 |
| 要望が出る前の先回り実装 | （踏む前に止める型として確立。「要望が出たら検討」を残課題に書く） | v231 ほか多数 |

---

## 5. 判断が覆る力学 — 転換点の共通構造

過去の決定を覆した約30件 (v80, v103, v106, v109, v112, v118, v121, v128, v143, v150, v151, v163, v174→175, v191→192, r29, r48, r58, r62, r68, r77, r89 ほか) の引き金は3種にほぼ収束する:

1. **実機で触った本人の一言** — 最頻。「見つからない」「複雑になるだけ」「精度がおちますね」。翌日どころか同日に覆すこともためらわない (v57→v58, v174→v175)。
2. **外部システムの現実** — Apple 静的スキャン (v106)、iPad スクショ要件 (v109)、ITP (r4)、WebView のバージョン差 (v216)。
3. **検証 YES によるリスク前提の消滅** — FGS 実機 E2E が通ったので「Android v1 クリーン」方針を上書き (v150)、手触りが当たったので入口を昇格 (v155)。

覆し方にも型がある: **データ・内部構造は温存し、表示・動線・入口だけ差し替える** (v151 deck 温存, v129 フラグ不変, v145 DB 名不変)。撤回は必要な粒度だけ (§1⑤)。そして必ず日付つきで記録する (§1⑦)。

**逆に、推測・レビュー・外部の助言だけで方針が覆った例はほぼない。** 証拠 (実機・実測・原典) が来るまで待ち、来たら即座に動く。

---

## 6. 人物像 — 判断者としての輪郭（事実からの要約）

- **役割分担が明確**: 本人=体験の定義・仕様アイデア・手触り判定・スコープ判断。Claude=実装分解・調査裏取り・検証・制約対処。本人の設計直感 (「線はロジックで」v187、「ID管理→確定で独立」v206、「入口で基準を変える」v62) がそのまま最小実装になることが繰り返し記録されている。
- **「削る人」**: 判断基準が最も濃く出るのは削る指示 —「余計」「不要」「複雑になるだけ」「〜だけで十分」。情報を足す提案には一貫して抑制的で、体験の中心 (写真・記録) に意識が向くことを守る。
- **欲望を殺さず、待たせる**: やりたいことは park リストと Notion 構想ページに全部残す。捨てるのではなく「検証 YES＋明示要求」まで寝かせる。だから un-park の瞬間は速い。
- **学習が複利で効く**: 1つ目 (あの日) は spike 卒業まで75日/v75。2つ目 (レシート) は初日にフルループ、11日で r91、8日目に審査台本まで。差は能力でなく「判断の移植」— 過去の自分のリポの実測教訓と判断理由を初日に写すから。
- **原則は進化する**: 「写真は端末から出ない」(絶対) → 「本人が選んだ相手以外には出ない・残らない」(共有の再定義, 2026-05-30) → 「データの機微度と被害の質で決める」(レシート BYOK, 2026-08-10)。原則を壊すのではなく、適用条件を言語化して精緻化する。curation 境界も「自覚的にまたぐ」と記録してからまたぐ (v121)。
- **正直さを製品価値として扱う**: 0件表示・課金の明示・「分かったふりをしない」AI 設計・ストリークや通知を真似しない逆張り。これは倫理であると同時にポジショニング (r82「逆張りが差別化の本体」)。

---

## 7. 事実サマリ（このモデルの根拠データ）

- あの日: v1 (2026-05-28) → v233 (2026-08-10)。spike 卒業 v75 (2026-06-17)、App Store デビュー 2026-07-01 (1.0 build21)、世界版 2026-07-03、iOS 1.6 公開 2026-07-16、Play 内部テスト vc4。セッション 1〜44 が TODO に記録。
- レシート: r1 (2026-08-10) → r91 (2026-08-20)。11日間。TestFlight 4日目、審査台本 8日目。
- 一次データ: `CHANGELOG.md` (背景/設計判断/ハマり/教訓の全記録)、`receipt-spike/CHANGELOG.md`、`TODO.md` (現在地・今は作らないリスト)、Notion (WHY/HOW・動画版構想・ライフログ)。
- 本文書の全出典 (vN/rN) は上記 CHANGELOG の該当版で原文が読める。

## 8. 使い方と更新ルール

- **本人**: 迷ったら §3 チェックリスト → 該当する §2 の文脈。新しいプロダクトを始める時は §2.1 と §6 を読み直す。
- **Claude**: セッションで機能提案・スコープ判断・設計相談をする前にこの文書を参照し、提案には該当する原則/出典を添える。この文書と実際の判断がズレたら、それは転換のサイン — 上書きを提案する。
- **更新**: 大きな判断・転換・新しいアンチパターンが出た版で該当節に1行追記（出典必須）。全面改訂は不要 — 一次データは CHANGELOG にあり、これは蒸留。追記が増えて読みにくくなったら v214 の BUILD と同じ発想で刈り込む。

---

*初版: 2026-08-21。あの日 v1〜v233・レシート r1〜r91・TODO・Notion (WHY/HOW/動画版/ライフログ) の全読解から抽出。*
