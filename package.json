{
  "name": "anohi-madeleine",
  "version": "1.0.0",
  "private": true,
  "description": "あの日 (Madeleine) — A Certain Day: Revived through Photographs and Footprints",
  "scripts": {
    "sync:web": "node scripts/sync-web.mjs",
    "cap:copy": "npm run sync:web && cap copy",
    "cap:sync": "npm run sync:web && cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0"
  }
}
